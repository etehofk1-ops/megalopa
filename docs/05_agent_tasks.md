# Agent Tasks

## Completed in MVP scaffold

- Create project structure
- Implement deterministic analyzer modules
- Create sample dog ontology pack
- Generate Markdown report
- Connect Next.js API to Python analyzer
- Build SaaS-style frontend without emoji
- Add pytest coverage

## Next tasks

1. Add file picker upload handling for browser `File` objects.
2. Add JSON schema validation using `jsonschema`.
3. Add downloadable Markdown report action.
4. Add optional LLM-assisted bias and relation suggestion layer.
5. Add graph review visualization with SVG or canvas.
